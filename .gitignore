# Rust build artifacts
/target

# Runtime logs (hundreds of auto-generated files)
/logs

# Inference / quantization output artifacts (large data files)
/outputs

# Build-generated SPIR-V shaders (committed to repo; build.rs skips
# recompilation when spv is up-to-date, so CI does not need Vulkan SDK)
# /assets/shaders/spv

# Model files (large, fetched separately)
*.st

# Test / example artifacts
state.bin

# IDE / editor config
/.trae

# Python caches
__pycache__/
*.pyc

# Embedded reference repo (Albatross; tracked separately upstream)
/参考/Albatross