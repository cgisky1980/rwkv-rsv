# Rust build artifacts
/target

# Runtime logs (hundreds of auto-generated files)
/logs

# Inference / quantization output artifacts (large data files)
/outputs

# Build-generated SPIR-V shaders (regenerated by build.rs via glslangValidator)
/assets/shaders/spv

# Model files (large, fetched separately)
*.st

# Test / example artifacts
state.bin

# IDE / editor config
/.trae

# Python caches
__pycache__/
*.pyc

# Embedded reference repo (Albatross; tracked separately upstream)
/参考/Albatross